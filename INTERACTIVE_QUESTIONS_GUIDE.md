# 互動式練習題 - 使用說明

## 功能概述

將靜態的練習題轉換為**互動式答題系統**，讓用戶可以：
- 選擇答案
- 提交後查看對錯
- 查看正確答案和詳細解析

## 功能特性

### 1. 答題流程

```
顯示題目和選項
    ↓
用戶選擇答案（選項高亮）
    ↓
點擊"提交答案"按鈕
    ↓
顯示對錯結果
    ↓
顯示正確答案
    ↓
顯示詳細解析
```

### 2. 視覺反饋

#### 選擇狀態
- **未選擇**：灰色邊框
- **已選擇（未提交）**：藍色邊框，藍色背景
- **正確答案（已提交）**：綠色邊框，綠色背景，✓ 圖標
- **錯誤答案（已提交）**：紅色邊框，紅色背景，✗ 圖標

#### 結果提示
- **答對**：✓ 答對了！（綠色卡片）
- **答錯**：✗ 答錯了（紅色卡片）

## 界面佈局

```
┌────────────────────────────────────────┐
│ 📖 教材內容                             │
│ （內容...）                             │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│ ✏️ 本章練習題                          │
├────────────────────────────────────────┤
│                                         │
│ ┌──────────────────────────────────┐  │
│ │ ① 選擇題                          │  │
│ │                                    │  │
│ │ 題目內容...                       │  │
│ │                                    │  │
│ │ ┌────────────────────────────┐   │  │
│ │ │ A) 選項A                    │   │  │
│ │ └────────────────────────────┘   │  │
│ │ ┌────────────────────────────┐   │  │
│ │ │ B) 選項B  ✓（正確）        │   │  │
│ │ └────────────────────────────┘   │  │
│ │ ┌────────────────────────────┐   │  │
│ │ │ C) 選項C                    │   │  │
│ │ └────────────────────────────┘   │  │
│ │                                    │  │
│ │ ┌──────────────────────────┐     │  │
│ │ │ ✓ 答對了！              │     │  │
│ │ └──────────────────────────┘     │  │
│ │                                    │  │
│ │ 正確答案：B                       │  │
│ │                                    │  │
│ │ 📝 詳細解析：                     │  │
│ │ 解題步驟說明...                   │  │
│ └──────────────────────────────────┘  │
│                                         │
│ ┌──────────────────────────────────┐  │
│ │ ② 計算題                          │  │
│ │ ...                                │  │
│ └──────────────────────────────────┘  │
└────────────────────────────────────────┘
```

## 技術實現

### 1. Markdown 解析

```javascript
parseQuestions(markdown) {
  // 解析 Markdown 格式
  // 識別：## 第X題（題型）
  // 識別：A) B) C) D) 選項
  // 識別：**正確答案：** 
  // 識別：**詳細解析：**
  // 識別：--- 分隔線
}
```

### 2. 狀態管理

```javascript
const [questions, setQuestions] = useState([]);        // 題目列表
const [userAnswers, setUserAnswers] = useState({});    // 用戶答案
const [showResults, setShowResults] = useState({});    // 是否顯示結果
```

### 3. 答題邏輯

```javascript
// 選擇答案
handleSelectAnswer(questionIndex, selectedOption) {
  setUserAnswers({
    ...userAnswers,
    [questionIndex]: selectedOption
  });
}

// 提交答案
handleSubmit(questionIndex) {
  setShowResults({
    ...showResults,
    [questionIndex]: true
  });
}

// 檢查是否正確
isCorrect(questionIndex) {
  return userAnswers[questionIndex] === questions[questionIndex].answer;
}
```

## 支援的題目格式

### 選擇題

```markdown
## 第1題（選擇題）
小明的速率是多少？

A) 5 m/s
B) 10 m/s
C) 15 m/s
D) 20 m/s

**正確答案：** B

**詳細解析：**
根據公式 $v = \frac{d}{t}$...

---
```

### 判斷題

```markdown
## 第2題（判斷題）
速率有方向性。

A) 正確
B) 錯誤

**正確答案：** B

**詳細解析：**
速率是純量，沒有方向...

---
```

### 填充題

```markdown
## 第3題（填充題）
1 m/s = ___ km/h

A) 3.6
B) 36
C) 0.36
D) 360

**正確答案：** A

**詳細解析：**
換算過程...

---
```

## 樣式設計

### 顏色方案

| 狀態 | 邊框 | 背景 | 文字 |
|------|------|------|------|
| 未選擇 | `border-gray-300` | `bg-white` | `text-gray-700` |
| 已選擇（未提交） | `border-primary-500` | `bg-primary-50` | `text-primary-900` |
| 正確答案 | `border-green-500` | `bg-green-50` | `text-green-900` |
| 錯誤答案 | `border-red-500` | `bg-red-50` | `text-red-900` |

### 互動效果

- **hover** - 未提交時，懸停顯示邊框高亮
- **disabled** - 已提交後，禁用點擊
- **transition** - 所有狀態變化都有過渡動畫

## 用戶體驗

### 答題前
1. 看到題目和所有選項
2. 點擊選項進行選擇
3. 可以修改選擇（提交前）
4. 出現"提交答案"按鈕

### 答題後
1. 選項變為禁用狀態
2. 正確答案顯示綠色 + ✓
3. 錯誤答案（如果選了）顯示紅色 + ✗
4. 顯示對錯提示框
5. 顯示正確答案卡片
6. 顯示詳細解析

### 多題獨立
- 每題獨立作答
- 可以跳過某題不答
- 可以任意順序作答
- 每題的狀態互不影響

## 優勢

### ✅ 互動性強
- 即時反饋
- 視覺化結果
- 增加參與度

### ✅ 學習效果好
- 主動答題加深記憶
- 錯誤時查看解析
- 正確時獲得成就感

### ✅ 使用者友好
- 清晰的狀態指示
- 明確的操作流程
- 美觀的視覺設計

### ✅ 技術優勢
- 自動解析 Markdown
- 無需修改後端
- 純前端實現

## 擴展建議

### 1. 統計功能
```javascript
// 計算正確率
const correctCount = Object.keys(showResults).filter(i => isCorrect(i)).length;
const totalAnswered = Object.keys(showResults).length;
const accuracy = (correctCount / totalAnswered * 100).toFixed(0);
```

### 2. 重做功能
```javascript
const handleReset = () => {
  setUserAnswers({});
  setShowResults({});
};
```

### 3. 儲存進度
```javascript
// 使用 localStorage 儲存答題記錄
localStorage.setItem(`chapter_${chapterId}_answers`, JSON.stringify(userAnswers));
```

### 4. 計時功能
```javascript
const [startTime, setStartTime] = useState(Date.now());
const [endTime, setEndTime] = useState(null);
```

## 測試建議

### 1. 功能測試
- [ ] 選擇答案正常高亮
- [ ] 提交後顯示結果
- [ ] 正確答案標記正確
- [ ] 錯誤答案標記錯誤
- [ ] 解析正確顯示

### 2. 邊界測試
- [ ] 沒有選項的題目
- [ ] 只有2個選項
- [ ] 超過4個選項
- [ ] 答案格式不標準

### 3. 用戶體驗測試
- [ ] 點擊順暢
- [ ] 視覺反饋清晰
- [ ] 在移動設備上正常顯示

---

**現在練習題變成互動式的了！用戶可以答題、查看結果和學習解析。** 🎯✏️

