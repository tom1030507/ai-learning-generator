# 📁 專案結構總覽

```
nccu_genai/                              # 專案根目錄
│
├── 📄 README.md                         # 主要專案說明文件
├── 📄 SETUP_GUIDE.md                    # 快速安裝指南
├── 📄 PROJECT_STRUCTURE.md              # 本檔案 - 專案結構說明
├── 📄 .gitignore                        # Git 忽略檔案設定
│
├── 🔧 start_backend.bat                 # Windows 後端啟動腳本
├── 🔧 start_frontend.bat                # Windows 前端啟動腳本
│
├── 📂 backend/                          # 後端專案目錄
│   │
│   ├── 📄 README.md                     # 後端 API 說明文件
│   ├── 📄 requirements.txt              # Python 依賴套件清單
│   ├── 📄 .env.example                  # 環境變數範例檔案
│   ├── 📄 .env                          # 環境變數設定（需自行建立，不納入版控）
│   ├── 📄 .gitignore                    # 後端 Git 忽略設定
│   │
│   ├── 🐍 main.py                       # FastAPI 主程式與路由定義
│   ├── 🐍 config.py                     # 環境變數與設定管理
│   ├── 🐍 database.py                   # 資料庫連接設定
│   ├── 🐍 models.py                     # SQLAlchemy 資料庫模型
│   ├── 🐍 schemas.py                    # Pydantic 資料驗證模型
│   ├── 🐍 gemini_service.py             # Gemini API 整合服務
│   │
│   ├── 📂 venv/                         # Python 虛擬環境（自動生成）
│   └── 🗄️ learning_generator.db        # SQLite 資料庫檔案（執行後自動生成）
│
└── 📂 frontend/                         # 前端專案目錄
    │
    ├── 📄 README.md                     # 前端開發指南
    ├── 📄 package.json                  # Node.js 專案設定與依賴
    ├── 📄 vite.config.js                # Vite 建構工具設定
    ├── 📄 tailwind.config.js            # Tailwind CSS 設定
    ├── 📄 postcss.config.js             # PostCSS 設定
    ├── 📄 .gitignore                    # 前端 Git 忽略設定
    ├── 📄 index.html                    # HTML 進入點
    │
    ├── 📂 public/                       # 靜態資源目錄
    │
    ├── 📂 src/                          # 前端原始碼目錄
    │   │
    │   ├── 📄 main.jsx                  # React 應用程式進入點
    │   ├── 📄 App.jsx                   # 主應用程式元件
    │   ├── 📄 index.css                 # 全域樣式與 Tailwind 設定
    │   │
    │   ├── 📂 components/               # React 元件目錄
    │   │   ├── 🎨 GeneratorForm.jsx    # 輸入表單元件
    │   │   ├── 🎨 OutlineDisplay.jsx   # 大綱顯示元件
    │   │   ├── 🎨 ContentDisplay.jsx   # 教材顯示元件
    │   │   ├── 🎨 QuestionsDisplay.jsx # 題目顯示元件
    │   │   ├── 🎨 History.jsx          # 歷史記錄元件
    │   │   └── 🎨 LoadingSpinner.jsx   # 載入動畫元件
    │   │
    │   └── 📂 services/                 # API 服務層
    │       └── 🔌 api.js                # Axios API 呼叫封裝
    │
    └── 📂 node_modules/                 # Node.js 依賴套件（自動生成）
```

## 📋 檔案說明

### 根目錄檔案

| 檔案 | 用途 |
|------|------|
| `README.md` | 完整的專案說明、安裝指南、使用方法 |
| `SETUP_GUIDE.md` | 簡化的快速安裝指南，適合初學者 |
| `PROJECT_STRUCTURE.md` | 本檔案，專案結構總覽 |
| `.gitignore` | Git 版本控制忽略設定 |
| `start_backend.bat` | Windows 快速啟動後端腳本 |
| `start_frontend.bat` | Windows 快速啟動前端腳本 |

### 後端核心檔案

| 檔案 | 功能 | 關鍵內容 |
|------|------|----------|
| `main.py` | FastAPI 應用主程式 | API 路由、CORS 設定、錯誤處理 |
| `config.py` | 環境設定 | 讀取 .env、API Key 管理 |
| `database.py` | 資料庫連接 | SQLAlchemy 引擎設定、Session 管理 |
| `models.py` | 資料模型 | Generation 表結構定義 |
| `schemas.py` | 資料驗證 | Pydantic 模型、Request/Response 格式 |
| `gemini_service.py` | AI 服務 | Gemini API 整合、提示工程 |

### 前端核心檔案

| 檔案 | 功能 | 關鍵內容 |
|------|------|----------|
| `App.jsx` | 主應用程式 | 狀態管理、路由、頁面佈局 |
| `main.jsx` | 進入點 | React 初始化、DOM 掛載 |
| `index.css` | 全域樣式 | Tailwind 設定、自訂 CSS 類別 |

### 前端元件說明

| 元件 | 職責 | 父子關係 |
|------|------|----------|
| `GeneratorForm.jsx` | 收集用戶輸入（科目/年級/單元） | 被 App.jsx 使用 |
| `OutlineDisplay.jsx` | 顯示與編輯大綱 | 被 App.jsx 使用 |
| `ContentDisplay.jsx` | 顯示教材內容 | 被 App.jsx 使用 |
| `QuestionsDisplay.jsx` | 顯示練習題與答案 | 被 App.jsx 使用 |
| `History.jsx` | 管理歷史記錄列表 | 被 App.jsx 使用 |
| `LoadingSpinner.jsx` | 顯示載入動畫 | 被多個元件重複使用 |

## 🔄 資料流程

```
用戶輸入
    ↓
GeneratorForm.jsx
    ↓
api.js (generateOutline)
    ↓
Backend: /api/generate-outline
    ↓
gemini_service.py (生成大綱)
    ↓
database.py (儲存)
    ↓
OutlineDisplay.jsx (顯示)
    ↓
api.js (generateContent)
    ↓
Backend: /api/generate-content
    ↓
gemini_service.py (生成教材)
    ↓
ContentDisplay.jsx (顯示)
    ↓
api.js (generateQuestions)
    ↓
Backend: /api/generate-questions
    ↓
gemini_service.py (生成題目)
    ↓
QuestionsDisplay.jsx (顯示)
```

## 📦 自動生成的檔案/目錄

這些檔案/目錄會在安裝或執行後自動生成，**不需要**手動建立：

### 後端
- `backend/venv/` - Python 虛擬環境
- `backend/learning_generator.db` - SQLite 資料庫
- `backend/__pycache__/` - Python 編譯快取

### 前端
- `frontend/node_modules/` - Node.js 依賴套件
- `frontend/dist/` - 建構後的生產版本

## 🔐 需要手動建立的檔案

只有一個檔案需要手動建立：

- `backend/.env` - 從 `backend/.env.example` 複製，填入實際的 Gemini API Key

## 📊 檔案統計

- **Python 檔案**：6 個
- **JavaScript/JSX 檔案**：9 個
- **設定檔**：7 個
- **文件檔案**：4 個
- **總計**：26 個檔案

## 🎯 開發流程建議

### 修改後端 API
1. 編輯 `backend/main.py` 或相關模組
2. 後端會自動重新載入（`--reload` 模式）
3. 在 `http://localhost:8000/docs` 測試新 API

### 修改前端介面
1. 編輯 `frontend/src/` 下的元件
2. Vite 會自動熱更新（HMR）
3. 瀏覽器會立即反映變更

### 新增功能
1. **後端**：新增路由 → 實作邏輯 → 更新文件
2. **前端**：建立元件 → 串接 API → 整合到 App

## 🧪 測試建議

### 後端測試
- 使用 Swagger UI：`http://localhost:8000/docs`
- 使用 Postman 或 curl 測試 API

### 前端測試
- 在瀏覽器開發者工具檢查網路請求
- 使用 React DevTools 檢查元件狀態

## 📈 未來擴充方向

可以考慮新增的檔案/功能：

- `backend/tests/` - 後端單元測試
- `frontend/src/tests/` - 前端測試
- `docker-compose.yml` - Docker 容器化設定
- `backend/migrations/` - 資料庫遷移腳本
- `frontend/src/hooks/` - 自訂 React Hooks
- `frontend/src/context/` - React Context API
- `.github/workflows/` - CI/CD 設定

## 💡 最佳實踐

1. **不要修改自動生成的檔案**（如 node_modules、__pycache__）
2. **永遠不要提交 .env 到版本控制**
3. **保持模組化**：一個檔案專注一個功能
4. **遵循命名慣例**：元件使用 PascalCase，函式使用 camelCase
5. **寫註解**：特別是複雜的邏輯和 AI 提示

---

**更新日期**：2025-12-13
**版本**：1.0.0



